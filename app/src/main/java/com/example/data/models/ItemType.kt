package com.example.data.models

enum class ItemType(val label: String) {
    LOGIN("Login"),
    CARD("Card"),
    IDENTITY("Identity"),
    SECURE_NOTE("Secure Note"),
    SSH_KEY("SSH Key"),
    PASSKEY("Passkey")
}
